name: users-cd

on:
  workflow_dispatch:

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}

jobs:
  deploy-container:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials (Access Keys)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region:            ${{ secrets.AWS_REGION }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Ensure ECR repositories exist (idempotent)
        shell: bash
        run: |
          set -euo pipefail
          for repo in fcg-users-api fcg-users-worker; do
            aws ecr describe-repositories --repository-names "$repo" >/dev/null 2>&1 || \
              aws ecr create-repository --repository-name "$repo" >/dev/null
          done

      - name: Build & Push (users-api)
        uses: docker/build-push-action@v6
        with:
          # Dockerfile assume contexto no root do repo
          context: .
          file: ./src/api/Dockerfile
          push: true
          tags: |
            ${{ steps.login-ecr.outputs.registry }}/fcg-users-api:latest
            ${{ steps.login-ecr.outputs.registry }}/fcg-users-api:${{ github.sha }}

      - name: Build & Push (users-worker)
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./src/Worker/Dockerfile
          push: true
          tags: |
            ${{ steps.login-ecr.outputs.registry }}/fcg-users-worker:latest
            ${{ steps.login-ecr.outputs.registry }}/fcg-users-worker:${{ github.sha }}
